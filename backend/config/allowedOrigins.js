// List of allowed origins for CORS
// This whitelist contains the URLs of the frontend applications that are allowed to interact with the backend.
// IMPORTANT: Update this list whenever the frontend URLs change or new frontend applications are added.
const whitelist = ['http://localhost:3000'];

export default whitelist;